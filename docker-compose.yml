services:
  cdocs:
    container_name: compsaberdocs
    build:
      context: .
      dockerfile: Dockerfile
      target: nginx
    ports:
      - "3010:80"
    restart: unless-stopped
    mem_limit: 1G
    networks:
      - proxymoxy
    
networks:
  proxymoxy:
    external: true